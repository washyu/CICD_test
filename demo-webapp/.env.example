# API Configuration
REACT_APP_API_URL=/api

# Docker Registry
REGISTRY=your-registry-url

# Database Configuration
DB_HOST=mysql
DB_USER=root
DB_PASSWORD=password
DB_NAME=demodb

# Build Configuration
NODE_ENV=production
